/**
 * Occudule design tokens — semantic names for use in components.
 * Actual values live in app/globals.css (:root) for instant global swaps.
 */
export const themeLabels = {
  primary: "Deep slate — backgrounds, hero text",
  accent: "Cyan — gradients, highlights",
  success: "Emerald — metrics, trust",
  cta: "Orange — primary actions only",
} as const;
