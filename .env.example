# Postmark (waitlist + contact forms)
POSTMARK_SERVER_TOKEN=
POSTMARK_FROM_EMAIL=
WAITLIST_NOTIFY_EMAIL=
# Optional — defaults to WAITLIST_NOTIFY_EMAIL, then support@occudule.com
# CONTACT_NOTIFY_EMAIL=

# Optional: Postmark message stream (defaults to "outbound")
# POSTMARK_MESSAGE_STREAM=outbound

# ─── Zoho SalesIQ live chat (Contact page) ───────────────────────────────────
# Public widget embed code (browser script), NOT a Zoho OAuth/API secret.
#
# Where to find it:
#   1. Log in to https://salesiq.zoho.com (or .eu / .in for your data center)
#   2. Settings → Brands → [your brand] → Installation → Website
#   3. Copy the widgetcode value from the embed snippet, e.g.:
#      widgetcode:"f589f59481ce309ac19d5af4c8559db208871444ed36899ac8a2b22830d6940c"
#      → use: NEXT_PUBLIC_ZOHO_SALESIQ_WIDGET_CODE=f589f59481ce...
#
# If your Zoho account is not on the US data center, also set the widget host:
# NEXT_PUBLIC_ZOHO_SALESIQ_HOST=salesiq.zoho.eu
#
# Add to .env.local (never commit .env.local), then restart: npm run dev
NEXT_PUBLIC_ZOHO_SALESIQ_WIDGET_CODE=
# NEXT_PUBLIC_ZOHO_SALESIQ_HOST=salesiq.zoho.com
